# F 批次：仓库整洁与技术债收尾报告

> 分支：`chore/housekeeping`
>
> 起点：`main` / `a8070f491cd1992220a4191741ed02683800e4a2`
>
> 日期：2026-08-31

## 0. 范围与结论

本批只处理合规证据归档、静态缓存策略、测试清单口径与 QA 指标释义；未修改玩法、角色/环境运行资产或渲染实现。`app/` 与 `public/models/` 相对起点均为零 diff。两个预存第三方目录状态保持原样：`tools/third_party/MB-Lab` 仍为未初始化/未跟踪 gitlink 状态，`tools/third_party/makehuman-assets` 仍为脏 submodule；未执行 clean、reset、restore 或强制 submodule 更新。

## F-1 归档 MakeHuman 审计输入

状态：**完成**。

七项 Police 输入先在原 submodule 工作区逐字节复算，再复制到 `art-source/_Source/MakeHuman/AuditedInputs/`。复算值与 `docs/licenses/MAKEHUMAN_CORE_ASSETS_CC0.md` 原记录逐项一致，总原始字节为 **16,452,612 B**。

| 归档相对路径 | 字节 | SHA-256 |
|---|---:|---|
| `base/skins/textures/young_lightskinned_male_diffuse2.png` | 3,595,270 | `03efe1f6b0ae52429649dcefc9dcaef6058032f874a251169cc3e2ed473c3874` |
| `base/hair/short01/short01_diffuse.png` | 2,354,146 | `f34a0957184e3d1e911ed59245f874f8d4843f61ee44ff49f43edb4be0196949` |
| `base/clothes/male_casualsuit03/male_casualsuit03_normal.png` | 9,610,278 | `412c4610d3b2ea1cb04aa3c0715e747a7c9f61d865133b7d69f70eaa738cf99b` |
| `base/eyebrows/eyebrow001/eyebrow001.png` | 89,619 | `9940f7d0b1b223709a19b05156ba6f6e9e4dbdbced02d7574f4d51d72c58967b` |
| `base/eyelashes/eyelashes01/eyelashes01.png` | 91,600 | `4b69c0fff2648874460e9caf80c31413c444218a5c50afebc425aeaa65484a35` |
| `base/eyes/high-poly/high-poly.obj` | 100,882 | `da2493215b708a344c33dc72f2a9a5b8fa985dcc5a70ad3b208995cf871da8e1` |
| `base/eyes/materials/brown_eye.png` | 610,817 | `4659691c7295ad6206c78b003e5fd0e5f91dcd53032fa914a229bb48cabe424b` |

证据与门禁：

- `manifest.json` 固定 submodule 原始路径、归档路径、字节和完整 SHA；`README.md` 保存来源、CC0 边界、本地许可路径及禁止清理警告。
- 六个 PNG 由既有 `*.png` Git LFS 规则存储；暂存区逐项显示 LFS pointer 的 `oid sha256` 与 manifest 一致。
- OBJ 仅 100,882 B，作为普通 Git 文本保存：体积小、可审阅，无需扩大所有 `*.obj` 的 LFS 策略。
- `tests/makehuman-audited-inputs.test.mjs` 冻结恰好七项的独立预期表，并对 manifest 与工作树字节双重复算，避免“文件和 manifest 一起被替换”形成自循环。
- `docs/licenses/MAKEHUMAN_CORE_ASSETS_CC0.md` 与 `docs/23_版权与第三方素材登记册.md` 已把原始输入可复建条件改为已关闭，同时保留醒目的 submodule 保存警告。

## F-2 `/_next/static/*` 缓存规则

状态：**完成**。

`public/_headers` 新增 `/_next/static/*` 的 `public, max-age=31536000, immutable`；`tests/static-cache-policy.test.mjs` 将该路由加入与 `/models/*`、`/basis/*` 同强度的逐项断言。clean build 后 `public/_headers` 与 `dist/client/_headers` 均为 436 B 且 `cmp` 一致；部署目录实际包含 9 个 `_next/static` 文件。

体积影响：头文件本身较旧文件增加 **70 raw B**，但不属于 first-playable encoded-transfer 资源。首屏在本批施工前后均为 **6,255,237 B**，增量 **0 B**，距 6 MiB 红线余 **36,219 B**。

`art-source/reports/m2-size-audit.json` 原冻结值已落后当前主干 clean build：刷新后 `dist/client=14,642,103 B`、首屏 `6,255,237 B`。相对旧报告的 dist 差为 +420 B，其中 +70 B 来自本批 `_headers`，其余 +350 B 是当前框架产物相对旧冻结报告的既有漂移；阈值与判定代码均未改。`qa:m2-size:check` 已用刷新后的实测报告通过。

## F-3 以 Git 跟踪清单为资产合同

状态：**完成**。

`tests/art-source.test.mjs` 的母版/扩展名清单，以及 `tests/model-assets.test.mjs` 的运行 GLB 和 22 张 authoring PNG 清单，均改为从 `git ls-files -z` 获取。未跟踪的 iCloud/Dropbox 同步副本不再制造假失败；真正被跟踪的缺失、多余、重复和类型污染仍由原有精确集合、计数与文件存在性断言覆盖。`dist/client` 是构建产物而非 Git 输入，相关部署扫描仍按真实文件系统执行，没有错误改成跟踪清单。

### 变异测试矩阵

变异在 `/private/tmp/chasing-f3-mutation-20260831` 独立克隆中执行，工作分支母版未被改动。

| 变异 | 命令 | 实际失败证据 | 还原证据 |
|---|---|---|---|
| 删除已跟踪 `art-source/Environment/Interactive/Locker_Hero.blend` | `node --test tests/art-source.test.mjs` | exit 1；`ENOENT ... Locker_Hero.blend`，失败于 `compact engine-neutral source-art masters remain intact` | 文件还原后 `git diff --quiet` exit 0 |
| 复制并 `git add` 多余 `art-source/Environment/Interactive/Tracked_Extra_Master.blend` | 同上 | exit 1；`ERR_ASSERTION`，actual 清单明确多出 `Tracked_Extra_Master.blend` | 取消暂存并移除临时副本后 `git diff --quiet` exit 0 |

这两次失败证明测试不是简单忽略未预期文件，而是只忽略**未跟踪**同步副本，仍会捕获真实仓库合同变化。

## F-4 QA 阴影口径命名

状态：**完成**。

为满足 `app/` 零 diff，本批不改 QA 运行时对象和渲染实现，而在 `docs/22_M3_择优渲染优化报告.md` 明确冻结现有快照键的语义：

- `render.shadow`：场景遍历得到的理论可投影 caster 清单估算；
- `render.breakdown["shadow-pass"]`：实际进入 renderer 阴影 pass 的 calls/triangles 工作量。

新增回归断言同时绑定 QA 源码中的两个现有键及 docs/22 的两条定义，防止后续再次把理论遍历值当成实测 pass 工作量。历史证据 JSON 和消费脚本无需迁移。

## 回归与门禁证据

| 门禁 | 结果 |
|---|---|
| `npm run lint` | 通过；0 error / 0 warning（保留既有 Babel 大文件提示） |
| `npm run typecheck` | 通过 |
| `npm run build:clean` | 通过；产物结构保持，保留既有 chunk >500 kB 提示 |
| `npm test` | **609/609 pass，0 fail / 0 skip**；起点 607，只增不减 |
| `npm run art:runtime-gate:check` | 36 GLB / 59 KTX2；Validator errors=0，stddev failures=0 |
| `npm run qa:m2-size:check` | 通过；first playable 6,255,237 B |
| `npm run qa:m3-mutations` | 四项变异均 exit 1 被捕获；四项 `restoredDiffQuiet=true` |

依赖环境补充：仓库迁移后本地 `node_modules/@types` 曾残留 18 个名为 `… 2` 的同步副本，导致 TypeScript 假失败。它们是未跟踪、可重建的依赖缓存；执行 `npm ci --prefer-offline` 后按 lockfile 恢复 617 packages、0 vulnerabilities，仓库依赖文件无 diff。

## 最终范围核对

- `git diff main -- app/`：空。
- `git diff main -- public/models/`：空。
- MakeHuman submodule 脏状态保持原样且不在暂存区。
- MB-Lab 预存状态保持原样且不在暂存区。
- 未 push；等待独立复验。
